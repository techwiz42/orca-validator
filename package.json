{
  "name": "orca-validator-verifier",
  "version": "0.1.0",
  "private": true,
  "description": "ORCA verifier + mermaid compiler — used at runtime by the boot gate and diagrams",
  "dependencies": {
    "@orcalang/orca-lang": "^0.1.30"
  }
}
